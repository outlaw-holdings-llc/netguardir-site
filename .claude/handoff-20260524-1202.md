# Session handoff — netguardir.com — 2026-05-24 12:02

## Original session goal

Site work was driven by sibling-project changes — every time CertGuard shipped a major UI update or fix, the site mockup needed to match. This session ran four wrangler deploys, all `--branch=main`, plus a strategy decision on the licensing FAQ + pricing tiers.

## Trajectory

1. **Carry-over commit** — committed the Netwrix-style sitewide rebuild that was sitting uncommitted from the previous session (commit `5cfcb25`). Confirmed Cloudflare Pages production branch is `main`.
2. **Chain Map mockup swap** (commit `7f4abc2`) — when CertGuard shipped v0.1.39 with the new Cytoscape Chain Map, the existing Bulk Enrollment mockup on `/certguard/` went stale. Replaced it with a styled mockup of the new compound-CA-container view + side detail drawer. Saved memory rule [[netguardir-mirror-product]] so future sessions auto-mirror product changes without prompting.
3. **Internal-data scrub + pricing FAQ** (commit `f443e7e`) — when Chris asked for the product-level scrub of NMC/IESS/OCA-SBX, the site mockups had to match. Also took the strategy-discussion CertGuard pricing tiers (Solo/Team/Site $1,995/$5,995/$14,995) and put them into the homepage licensing FAQ.
4. **"Vessel" wording drop in vs-cyberark** (commit `ad82064`) — Chris flagged that V1/Vessel terms would identify his employer to colleagues. Replaced one prose mention ("Carry it onto a vessel or SCIF visit" → "Carry it into a SCIF or onto a deployed site").

## Final state

- **Live site:** https://netguardir.com (apex) + https://netguardir.com/certguard/ + https://netguardir.com/certguard/one-pager/ + https://netguardir.com/certguard/vs-cyberark/
- **Latest deploy URL fingerprint:** see `wrangler pages deploy . --project-name netguardir --branch=main` output for the unique `*.netguardir.pages.dev` ID (last was `43fa6979.netguardir.pages.dev`).
- **Local git state:** branch `master` (local-only — no remote). Latest commit `ad82064`.
- **Production branch on Cloudflare:** `main`. Always pass `--branch=main` explicitly to wrangler or the apex stays stale.
- **Verified live cleanliness:**
  - `curl -s https://netguardir.com/ | grep -cE 'V1-9407R|10\.50\.0\.|NMC|IESS|OCA-SBX'` → 0
  - `curl -s https://netguardir.com/certguard/ | grep -cE 'V1-9407R|10\.50\.0\.|NMC|IESS|OCA-SBX|nmc\.local|iess\.local'` → 0
  - `curl -s https://netguardir.com/certguard/vs-cyberark/ | grep -ciE 'vessel|V1[-_]'` → 0
- **License FAQ at `index.html:504-518`** lists CertGuard Solo/Team/Site tiers with prices + offline license-file model description + `licensing@netguardir.com` mailto CTA.

## What was tried and rejected (with reasons)

(none this session — every site change Chris requested landed cleanly first try)

## Open questions / blockers

- **Pricing + competitor pages workstream** is on the menu but not yet picked. Would add `/pricing/` (gated form behind Cloudflare Turnstile), 4 more `/vs-X/` pages (Venafi, Keyfactor, AppViewX, native AD CS `/certsrv`), `/compare/` matrix.
- **License-gated downloads** — swap open URLs in download tiles for activation-required signed URLs (Cloudflare Workers can sign; or a tiny Flask service on this VPS).

## "Re-read me first thing next session"

1. **Production branch is `main` NOT `master`.** Always `wrangler pages deploy . --project-name netguardir --branch=main --commit-dirty=true` or the deploy goes to a preview URL while the apex stays stale. See [[netguardir-pages-deploy]] for the exact command + token.
2. **Local repo has NO git remote** — deploys are via wrangler, not git push.
3. **Commits require `-c user.name=... -c user.email=...` flags** — the repo lacks a local user config, and per [[destructive-ops-manual]] we don't modify global git config. One-shot override per commit is the workaround. Use `Chris Outlaw <chris.outlaw@protonmail.com>` to match prior commit authorship.
4. **Cloudflare token:** `api_token_full` at `/root/.console-creds/cloudflare.json`. Needs Pages:Edit + Account Settings:Read.
5. **Site mockup must mirror product UI on every major change** — saved as [[netguardir-mirror-product]] this session. Don't wait to be asked.
6. **Downloads NOT served by Cloudflare Pages** — they live at `/var/www/netguardir-downloads/` on this VPS, served by nginx, fronted by Cloudflare DNS (gray-cloud, not orange). Refresh via atomic `cp foo.zip foo.zip.new && mv foo.zip.new foo.zip`.

## File index — exact paths touched this session

- `assets/styles.css` — appended Chain Map mockup CSS (~85 lines): `.cm-stats`, `.cm-tile`, `.cm-canvas`, `.cm-ca` (root/issuing/external/healthy/warn30/expired variants), `.cm-dot` (ok/urgent/exp/selected), `.cm-side`, `.cm-kv`.
- `certguard/index.html` — replaced GUI mockup block at lines 150-226 with Chain Map mockup; sanitized internal names; updated FAQ multi-domain answer
- `index.html` — sanitized NetGuard mockup (V1-9407R-1 → core-sw-01, 10.50.0.12 → 10.0.0.10); replaced Licensing FAQ at lines 504-518 with new CertGuard pricing tiers + offline license-file description
- `certguard/vs-cyberark/index.html` — replaced "vessel" wording at lines 131-135
- `WORKLOG.md` — appended this session entry
- `.claude/handoff-20260524-1202.md` — this file

**Commits this session:** `5cfcb25` (Netwrix-style rebuild carry-over) → `7f4abc2` (Chain Map mockup swap) → `f443e7e` (Internal-data scrub + pricing FAQ) → `ad82064` (Drop "vessel" wording)

**Deploys this session:** 4 wrangler deploys, all to production via `--branch=main`.
